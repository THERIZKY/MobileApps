import React from "react";
import { Text, View } from "react-native";

const menus = () => {
    return (
        <View>
            <Text>menus</Text>
        </View>
    );
};

export default menus;
